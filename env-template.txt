# ========================================
# ESL Explorers Environment Configuration
# ========================================

# Supabase Configuration
# Get these from your Supabase project dashboard
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Platform URLs
# Update these with your actual deployed platform URLs
NEXT_PUBLIC_STUDENT_URL=https://student.yourdomain.com
NEXT_PUBLIC_TEACHER_URL=https://teacher.yourdomain.com

# Site URL (for GitHub Actions revalidation)
# This should be your production URL (e.g., https://eflexplorers.com)
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# ========================================
# SETUP INSTRUCTIONS
# ========================================

# 1. Database Setup:
#    - Apply db/content-schema.sql in Supabase SQL editor
#    - Apply db/content-seed-simple.sql to populate initial content

# 2. Environment File:
#    - Copy this file to .env.local in project root
#    - Replace placeholder values with actual Supabase credentials
#    - Platform URLs should point to your deployed student/teacher apps

# 3. Supabase Configuration:
#    - Enable Row Level Security (RLS) on content tables if needed
#    - Set up authentication providers if using social login

# 4. Content Management:
#    - Marketing content is now database-driven
#    - Update content via Supabase dashboard or admin interface
#    - Components include fallback to hardcoded content during development